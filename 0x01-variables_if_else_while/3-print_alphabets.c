#include <stdio.h>

/**
 * main - Entry point
 *
 * Description:alphabet in lowercase, and then in uppercase using putchar
 *
 * Return: 0 (success)
*/

int main(void)
{
	ch = 'a'
	CH = 'A'

		/*lowercase*/
	while (ch <= 'z')
	{
		putchar = ('ch');
		ch++;
	}

		/*uppercase*/
	while (CH <= 'Z')
	{
		putchar = ('CH');
		CH++;
	}
	putchar = '\n';
	return (0);
}
